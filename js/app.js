const toggle=document.querySelector('[data-nav-toggle]');const nav=document.querySelector('[data-nav]');toggle?.addEventListener('click',()=>nav.classList.toggle('open'));document.addEventListener('click',(e)=>{if(e.target.closest('[data-nav-toggle]'))return;if(nav&&!e.target.closest('[data-nav]'))nav.classList.remove('open')});document.addEventListener('xr:object-selected',(e)=>console.debug('XR selected',e.detail));